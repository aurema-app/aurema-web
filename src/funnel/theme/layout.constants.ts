/** Bottom padding for pinned CTA footers — respects home-indicator safe area. */
export const FUNNEL_CTA_FOOTER_PB = "max(16px, env(safe-area-inset-bottom))";

/** Chakra spacing token for funnel step shell top padding. */
export const FUNNEL_STEP_TOP_PADDING = 2;
